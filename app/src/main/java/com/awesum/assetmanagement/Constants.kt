package com.awesum.assetmanagement

object Constants {
    // Demo job id matching the seed data in schema.sql (JOB-2026-0083).
    // In a real app this would come from a job list / login session.
    const val CURRENT_JOB_ID = 1
}
